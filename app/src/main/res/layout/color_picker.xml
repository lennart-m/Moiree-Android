<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:layout_height="match_parent"
    tools:layout_width="match_parent"
    tools:parentTag="RelativeLayout">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_alignParentBottom="true">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            android:src="@drawable/rainbow"/>

    </FrameLayout>

    <de.lennartmeinhardt.android.moiree.util.SeekPane
        android:id="@+id/hue_selection"
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_alignParentBottom="true"
        app:maxX="360"
        app:maxY="0"
        app:stepX="10"
        app:thumbDrawable="@drawable/thumb_rect"/>

    <de.lennartmeinhardt.android.moiree.util.SeekPane
        android:id="@+id/sat_val_selection"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/hue_selection"
        android:layout_marginBottom="20dp"
        android:clickable="true"
        android:focusable="true"
        app:maxX="300"
        app:maxY="300"
        app:stepX="10"
        app:stepY="10"
        tools:background="@drawable/sat_val_mask"/>

</merge>
